<%= form_tag  action: :sign_in, method: :post do | f |%>
  <table class="table">
    <tr>
      <td><%= label_tag :email %></td>
      <td><%= email_field_tag :email %></td>
    </tr>
    <tr>
      <td><%= label_tag :password %></td>
      <td><%= password_field_tag :password %></td>
    </tr>
    <tr>
      <td><%= submit_tag "Sign In", :class=> "btn btn-primary"%></td>
      <td></td>
   </tr>
  </table>
<%end%>
<!-- <%= link_to "See all records", users_path%> -->
<!--
_path => /users
_url => http://localhost:3000/users -->
